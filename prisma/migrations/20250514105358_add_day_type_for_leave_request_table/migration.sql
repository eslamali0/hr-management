BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[LeaveRequest] ADD [dayType] NVARCHAR(1000) NOT NULL CONSTRAINT [LeaveRequest_dayType_df] DEFAULT 'FULL_DAY';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
